@model IEnumerable<_420Project.Models.GroupFilter>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPanel.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ToDoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ToDoId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GroupFilterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GroupFilterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GroupFilterId })
        </td>
    </tr>
}

</table>
